"use client";

/**
 * OTP Verification (Legacy)
 * Ce composant a été déplacé vers `otp-flow.legacy.tsx` pour permettre
 * l'activation du flow de confirmation par lien email standard (EmailVerifyWaiting).
 */
export { default } from "./otp-flow.legacy";
